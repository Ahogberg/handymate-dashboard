import { NextRequest,NextResponse } from 'next/server';import { actor } from '@/lib/mail/auth';import { parseProvider,sameOrigin } from '@/lib/mail/security';import { syncMailbox } from '@/lib/mail/poll';export const maxDuration=180;export async function POST(r:NextRequest,c:{params:Promise<{provider:string}>}){try{if(!sameOrigin(r))return NextResponse.json({error:'Ogiltigt ursprung.'},{status:403});const a=await actor(r);return NextResponse.json(await syncMailbox(a.businessId,parseProvider((await c.params).provider)))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Synken misslyckades.'},{status:503})}}
